<?php
$targetDir = "uploads/";
$targetFile = $targetDir . basename($_FILES["file"]["name"]);

if (move_uploaded_file($_FILES["file"]["tmp_name"], $targetFile)) {
    echo "✅ File uploaded successfully!<br>";
    echo "<a href='$targetFile'>View file</a>";
} else {
    echo "❌ Upload failed.";
}
?>

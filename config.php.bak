<?php
$host = "localhost";
$db   = "student_portal";
$user = "portaluser";       // the user you just created
$pass = "StrongPassword";   // the password you set

$conn = new mysqli($host, $user, $pass, $db);

// check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>

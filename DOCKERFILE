# Use official PHP image with built-in server
FROM php:8.2-cli

# Copy project files
COPY . /var/www/html

# Set working directory
WORKDIR /var/www/html

# Expose port
EXPOSE 10000

# Start PHP built-in server on the port Render assigns
CMD ["php", "-S", "0.0.0.0:10000", "-t", "."]
